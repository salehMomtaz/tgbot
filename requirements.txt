pyrogram
tgcrypto
fastapi
uvicorn
# [default] keeps certifi/brotli extras; [curl-cffi] gives yt-dlp TLS/browser
# impersonation — REQUIRED for TikTok's proof-of-work webpage challenge solver
# (without it TikTok interstitials surface as "malformed site" failures).
yt-dlp[default,curl-cffi]
ffmpeg-python
aiohttp
aiofiles
apscheduler
requests
python-dotenv
# yt-dlp plugin that talks to the bgutil PO-token HTTP provider (Deno server).
# Auto-discovered by yt-dlp; the provider server itself is cloned by ./install.sh.
bgutil-ytdlp-pot-provider
# Direct-forward DM relay (modules/direct_forward/). Local libraries speaking
# the platforms' own private APIs — no third-party relay services.
instagrapi
# curl-backed requests adapter giving instagrapi's PRIVATE session a real
# Chrome TLS fingerprint (utils/ig_anti_detect.py). The private API previously
# rode plain Python requests TLS — an easy "this is a script" signal.
curl-adapter>=1.2.1
twikit
# Async WebSocket client for the TikTok IM push channel
# (_tiktok_worker in modules/direct_forward/). Also pulled in by
# yt-dlp[default], but declared here because we import it directly.
websockets
