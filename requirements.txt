# kurigram is an actively-maintained drop-in replacement for the archived
# upstream pyrogram (kurigram ships its code under the 'pyrogram' namespace
# at install time — verified via the wheel RECORD and the project README;
# 'import pyrogram' loads kurigram). POLICY (AGENTS.md): always upgrade to
# the newest stable kurigram release. Currently tracking >= 2.2.25 (released
# 2026-08-21). On every fresh install / `pip install -U -r requirements.txt`
# the newest compatible kurigram is pulled in automatically; the policy's
# job is to KEEP that flow — never re-pin to an exact older version.
kurigram>=2.2.25
tgcrypto
fastapi
uvicorn
python-multipart
# [default] keeps certifi/brotli extras; [curl-cffi] gives yt-dlp TLS/browser
# impersonation — REQUIRED for TikTok's proof-of-work webpage challenge solver
# (without it TikTok interstitials surface as "malformed site" failures).
yt-dlp[default,curl-cffi]
# PIN curl_cffi < 0.14: yt-dlp's TikTok extractor hardcodes impersonate=True,
# which resolves to curl_cffi's NEWEST chrome target. curl_cffi >= 0.14 ships
# chrome142+ TLS fingerprints that TikTok now blocks ("Site Maintenance" page →
# "Unexpected response from webpage request", yt-dlp#17403). 0.13.x's newest
# chrome is 131 (136/133 are yt-dlp-deprioritized), which TikTok accepts. The
# bot venv MUST stay on this pin — utils/updater.py enforces it on every nightly
# refresh. If TikTok hardens chrome131 too, bump the pin back after confirming
# the newer target works again.
curl_cffi<0.14
ffmpeg-python
aiohttp
aiofiles
apscheduler
requests
python-dotenv
# yt-dlp plugin that talks to the bgutil PO-token HTTP provider (Deno server).
# Auto-discovered by yt-dlp; the provider server itself is cloned by ./install.sh.
bgutil-ytdlp-pot-provider
# Direct-forward DM relay (modules/direct_forward/). Local libraries speaking
# the platforms' own private APIs — no third-party relay services.
# 2.18.14 brings Realtime MQTToT (Instagram push, TikTok-like). Keep in sync with aiogram.
instagrapi==2.18.14
# curl-backed requests adapter giving instagrapi's PRIVATE session a real
# Chrome TLS fingerprint (utils/ig_anti_detect.py). The private API previously
# rode plain Python requests TLS — an easy "this is a script" signal.
# 1.2.x requires curl_cffi>=0.14 (breaking the TikTok pin above); 1.1.0 works
# with curl_cffi 0.13.x. Keep in sync with the curl_cffi pin.
curl-adapter==1.1.0
twikit
# Async WebSocket client for the TikTok IM push channel
# (_tiktok_worker in modules/direct_forward/). Also pulled in by
# yt-dlp[default], but declared here because we import it directly.
websockets
# Bale.ai frontend (optional, aiogram v3 → https://tapi.bale.ai). Only needed if BALE_TOKEN is set.
# 3.30.0+ supports pydantic 2.13 needed by instagrapi 2.18.14's Realtime.
aiogram==3.30.0
# Sequential headless cookie refresher (utils/cookie_refresher.py, Phase 26).
# Optional-ish: the refresher degrades gracefully (logs "playwright not installed
# — skip"), but without it DM-only jars (IG/X/TT/YT) go stale ~7 days. The
# Chromium browser itself is installed by install.sh (`python -m playwright
# install chromium`), not pip. Keep in sync with the pinned browsers.json that
# `playwright install` reads.
playwright
